[DEFAULT]
sources = ./migrations
migration_table = _yoyo_migration
batch_mode = off
verbosity = 3

;this file sets default configuration for yoyo-migrations cli
;to use migrations cli create new file named yoyo.ini,
;inherit this file and set custom config params

;EXAMPLE OF yoyo.ini (copy without semicolons)

;[DEFAULT]
;%inherit = yoyo-defaults.ini
;database = postgresql://user:password@hostname:port/database
